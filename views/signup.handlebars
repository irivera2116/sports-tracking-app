<div class="flex justify-center items-center h-screen text-slate-700">
  {{! <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="/"><b>SPORTS CHAT ROOMS</b></a>
    </div>
  </nav> }}
  {{!-- to commit --}}

  <div id="first" class="home-container">
    <form class="w-96 p-6 shadow-lg bg-white rounded-md">
      <h1 class="font-bold text-center text-2xl text-gray-600">
        <i class="fa-solid fa-medal" style="color: #050505;"></i>
        SIGN UP
        <i class="fa-solid fa-medal" style="color: #050505;"></i>
      </h1>

      <div class="form-group">
        <label for="username" class="block text-base mb-2">Username:</label>
        <br />
        <input
          class="border w-full text-base px-2 py-1 focus:outline-none focus:ring-0 focus:border-gray-500"
          id="username"
          type="text"
          onblur="checkUsername()"
          placeholder="Username"
        />
        {{!-- <span id="error1" class="d-none">Username already taken!</span> --}}
        {{!-- <span id="error1b" class="d-none">You must type in a username.</span> --}}
        {{!-- <span id="error1c" class="d-none"> No space allowed.</span> --}}
        {{!-- <span id="noerror1" class="d-none">The username is available.</span> --}}
      </div>

      <div class="mt-4">
        <label for="username" class="block text-base mb-2">First Name:</label>
        <br />
        <input
          class="border w-full text-base px-2 py-1 focus:outline-none focus:ring-0 focus:border-gray-500"
          id="firstname"
          type="text"
          placeholder="First name"
        />
        {{!-- <span id="error4" class="d-none">You must give your name.</span> --}}
      </div>

      <div class="mt-4">
        <label for="username" class="block text-base mb-2">Last Name:</label>
        <br />
        <input
          class="border w-full text-base px-2 py-1 focus:outline-none focus:ring-0 focus:border-gray-500"
          id="lastname"
          type="text"
          placeholder="Last name"
        />
        {{!-- <span id="error5" class="d-none">You must give your last name.</span> --}}
      </div>

      <div class="mt-4">
        <label for="password" class="block text-base mb-2">Password:</label>
        <br />
        <input
          class="border w-full text-base px-2 py-1 focus:outline-none focus:ring-0 focus:border-gray-500"
          id="password"
          type="password"
          placeholder="Password"
        />
      </div>
      <div class="mt-4">
        <label for="passwordConfirm" class="block text-base mb-2">Confirm
          Password:</label>
        <br />
        <input
          class="border w-full text-base px-2 py-1 focus:outline-none focus:ring-0 focus:border-gray-500"
          id="passwordConfirm"
          type="password"
          placeholder="Password"
        />
        {{!-- <span id="error3" class="d-none">Passwords do not match or are blank.</span> --}}
      </div>

      <!-- Button trigger modal -->
      <div id="register" class="mt-4"></div>
        <button
          onClick="register(event)"
          type="button"
          class="bg-slate-700 text-zinc-300 w-full border-2 border-slate-700 rounded-lg hover:bg-transparent hover:font-extrabold hover:text-slate-700"
          >Register
        </button>
      </div>
      {{!-- <button
        id="continue"
        type="button"
        class="bg-slate-700 text-zinc-300 w-full border-2 border-slate-700 rounded-lg hover:bg-transparent hover:font-extrabold hover:text-slate-700"
        data-bs-toggle="modal"
        onClick="checkUser(event)"
      >Continue
      </button> --}}
    </form>
  </div>
</div>

<!-- Modal Avatar -->
<div
  class="modal fade"
  id="staticBackdrop"
  data-bs-backdrop="static"
  data-bs-keyboard="false"
  tabindex="-1"
  aria-labelledby="staticBackdropLabel"
  aria-hidden="true"
>
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="staticBackdropLabel">Choose Your Avatar</h5>
        <button
          type="button"
          class="btn-close"
          data-bs-dismiss="modal"
          aria-label="Close"
        ></button>
      </div>
      <div id="modal-body" class="modal-body row text-center">
      </div>
      <div class="modal-footer">
        <div id="register" class="text-center">
          {{!-- <button
            type="button"
            class="btn btn-secondary"
            data-bs-dismiss="modal"
          >Close</button> --}}
          {{!-- <button
            onClick="register(event)"
            type="button"
            class="btn btn-primary"
          >Register</button> --}}
          <span id="error4" class="d-none">Choose an avatar to register.</span>
        </div>
      </div>
    </div>
  </div>

  <script src="./javascript/signup.js" defer></script>
</div>